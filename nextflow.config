profiles {
    standard {
        executor = 'local'
        docker.enabled = true
    }
    lmod {
        executor = 'standard'
        docker.enabled = false
        process {
            module = 'sratoolkit:atropos:fastqc:multiqc'
        }
    }
}
