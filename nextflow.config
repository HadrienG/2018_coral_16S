profiles {
    standard {
        executor = 'local'
        docker.enabled = true
    }
    lmod {
        executor = 'local'
        docker.enabled = false
        process {
            module = 'sratoolkit:atropos:fastqc:multiqc:gcc/5.2.0'
        }
    }
}
